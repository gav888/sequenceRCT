# sequenceRCT 0.2.0

- Bumped package version to 0.2.0.
- Updated README version badge to 0.2.0.

# sequenceRCT 0.1.1

- Added multi-group support in `test_complexity_measures()` using Kruskal-Wallis tests when more than two groups are present.
- Expanded `Readme.md` with clearer package scope, workflow, and usage guidance.
- Added package citation support via `inst/CITATION` for `citation("sequenceRCT")`.
